//cpp
#include "loops.h"

int get_num_factorial(int num);
{
    int sum = 1;

    int index = 1

    while (index <= num)
    {
        sum = sum * index;

        index++;

        
    }

    return sum;
}